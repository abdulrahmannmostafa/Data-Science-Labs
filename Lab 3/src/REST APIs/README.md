# Report of Findings

## Task 1

I found that from all repos `scikit-learn`, `pytorch`, and `tensorflow` that the oldest repo is that of `scikit-learn`, while the the most stars was `tensorflow`, and finally the repo was the most issues is `pytorch`.

## Task 2

I fetched the repos of my Github account `abdulrahmannmostafa`, and I found that the most language I have used is **Jupyter Notebook**, average stars is **0.609375**, most recently updated repo is **Data-Science-Labs**, and oldest repo is **Paint-For-Kids**.

## Task 3

I fetched repos which concerns about **Data Science** with **Python Language** and **Stars >= 500**. I compared between `pandas-dev/pandas`, `scikit-learn/scikit-learn`, `tensorflow/tensorflow`, `pytorch/pytorch`, `keras-team/keras` and found that the repo with the most stars, and forks is `tensor-flow/tensorflow` while the oldest repo is `scikit-learn/scikit-learn`.
